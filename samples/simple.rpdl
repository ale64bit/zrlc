pipeline Pipeline(): vec4 {

  def vertex(pos: vec3): vec3 {
    builtin.position = vec4(pos, 1.0)
    return pos
  }

  def fragment(pos: vec3): vec4 {
    return vec4(pos, 1.0)
  }
}

renderer Simple(geometry: set) { 
  for g in geometry {
    builtin.screen += Pipeline(pos = g)
  }  
}

